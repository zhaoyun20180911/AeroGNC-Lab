# Development Log / 开发记录

## 2026-08-17 — 1.0 基线 / Baseline

- 建立 CMake/C++20 工程、独立核心库、自动测试和原生 Win32 桌面应用。
- 实现向量、完整 3×3 矩阵、标量在前四元数、刚体欧拉方程和 RK4。
- 完成卫星二体轨道/LVLH 姿态控制及早期火箭 6DOF/TVC 演示模型。
- 完成深色桌面 GUI、实时曲线、性能摘要和 CSV 导出。

## 2026-08-18 — 2.0 数据与火箭任务 / Mission data and launch vehicle

- 将七个用户提供的 CSV 原样移入 `data/rocket_missions/`，保留文件内容与 SHA-256。
- 新增任务仓库，固定 3 个发射场 × 2 个目标轨道，不允许构造第七种任务。
- 读取 ECI 位置/速度、参考四元数、参考角速度、质量、推力、级段和气动量；四元数先进行符号连续处理，再使用 SLERP。
- 新增以 CSV 首状态为初值的 ECI 火箭真值模型，支持质量/惯量/推力/Isp/Cd/密度/TVC 零偏。
- 实现一级分离、二级动力段及文件结束后的标称/实际独立二体轨道滑行。

## 2026-08-18 — 2.0 控制、扰动与卫星 / Control, disturbance and satellite

- 抽取卫星/火箭共用控制器，支持 OFF、默认、自定义 PD/PID/LQR；PID 包含积分限幅、抗饱和和重置清零。
- 推荐增益由飞行器类型、当前惯量与执行机构力矩能力计算，火箭推荐值同时考虑真值模型偏差。
- 抽取可组合持续扰动模块；随机过程使用用户种子和固定更新时间，可重复验证。
- 卫星扩展为六个经典轨道根数、四类姿态任务、飞轮惯量/力矩/转速配置。
- 加入卫星摄动占位设置；占位项不进入当前二体动力学。

## 2026-08-18 — 2.0 GUI 与验收 / GUI and acceptance

- 全面替换 1.0 主窗口，所有可见参数、选项、按钮、状态、指标与设置均中英双语。
- 新增控制、扰动、摄动三个模态设置窗口；火箭场景禁用摄动入口。
- 新增卫星任意轨道 3D 投影、火箭标称/实际 3D 轨迹、误差连线、多相机、放大/还原。
- 四个图表各自提供变量下拉选择；加入固定播放速度与最快模式。
- 自动测试扩展到 23 项，覆盖六个火箭任务全程、轨道滑行、控制关闭隔离、扰动组合/确定性和四种卫星任务。
- Release 构建、自动测试、主界面截图和三个设置弹窗截图均完成。

## 2026-08-18 — 2.0 可视化与阶段语义修订 / Visualization and phase semantics

- 产品显示名统一为 `AeroGNC Lab v2.0｜航天器GNC仿真与控制实验平台`，移除主界面冗余副标题和设置摘要行。
- 控制设置窗口扩宽并重新分栏，X/Y/Z 参数矩阵与推荐增益区域不再重叠。
- 3D 技术视图增加鼠标旋转、滚轮缩放、右键平移和双击复位；卫星地球与实轨采用统一物理比例。
- 火箭增加地心全球轨道视图，入轨时自动从局部上升切换，展示上升轨迹、入轨点、目标轨道和滑行弧段。
- 入轨后停止生成爬升跟踪误差，入轨指标固定为首次滑行样本；新增实际轨道高度和轨道根数曲线。
- 图表采用友好刻度、显式零线和变量相关的非负/有符号范围，质量等非负量从零起算。
- 自动测试增加入轨指标冻结用例；Release 共 24 项测试通过。

## 2026-08-18 — v2.1 版本标识 / Version identity

- 当前产品显示名更新为 `AeroGNC Lab v2.1｜航天器GNC仿真与控制实验平台`。
- 主窗口、三个设置窗口、工程版本和当前说明文档统一更新为 v2.1；v2.0 开发记录作为历史保留。

## 2026-08-18 — v2.1 火箭轨迹制导 / Rocket trajectory guidance

- 修复推力偏差下跨级消耗质量、推进剂耗尽后仍持续推力的严重问题；两级推进剂改为独立状态并在耗尽时强制关机。
- 新增位置/速度轨迹制导外环，向原有 PD/PID/LQR 姿态内环提供受修正角和指令变化率约束的姿态指令。
- 新增终端轨道比能量制导、自适应提前关机和有限补燃，不引入在线轨迹优化器。
- 新增双语制导设置窗口、主界面制导状态/推进剂指标、四类制导曲线和扩展 CSV 字段。
- 入轨快照改为实际关机事件；终端轨道根数相对固定目标轨道计算，入轨后爬升误差停止累计。
- 自动测试扩展至 27 项，包含六任务 ±5% 推力偏差、推进剂/关机、闭环优于开环及内环增益影响轨迹验证。

## 2026-08-18 — v3.1 卫星轨道控制与界面层级 / Satellite orbit control and UI hierarchy

- 当前产品显示名更新为 `AeroGNC Lab v3.1｜航天器GNC仿真实验平台`；窗口标题、设置窗口、工程版本和当前说明文档同步更新。
- 主界面将“卫星 / Satellite”和“运载火箭 / Launch Vehicle”提升为独立场景选择行；下方功能行按场景显示“姿态控制”“轨道控制/轨迹制导”“扰动设置”“摄动设置”。
- 原“控制设置 / Control”统一改名为“姿态控制 / Attitude Control”，明确 PD/PID/LQR 属于转动内环。
- 卫星新增独立名义/实际二体轨道、RTN 六状态轨道反馈、三轴微推力器组、推力限幅、比冲/推进剂消耗与耗尽强制停机。
- 卫星扰动新增指定时刻的一次性 RTN/LVLH Δv 脉冲；主界面、三维视图、性能指标、曲线和 CSV 同步增加轨道误差、轨控推力、推进剂及累计 Δv 数据。
- 自动测试扩展至 28 项；0.20 m/s 航向脉冲用例验证闭环 1200 s 末位置误差约 0.21 m，显著小于开环约 251.5 m，并覆盖推力上限与推进剂耗尽语义。
- 新增 Windows x64 免安装构建：MSVC 运行库静态链接，七个原始任务 CSV 作为 RCDATA 嵌入 EXE，并保留开发环境外部数据优先、内嵌资源回退的加载策略。
- EXE 写入 3.1.0.0 文件/产品版本信息，提供内嵌资源无界面自检、单文件输出、便携 ZIP、SHA-256 清单和可重复打包脚本。
